{
  "name": "orders-settlements",
  "private": true,
  "version": "0.1.0",
  "description": "Orders and settlements — orders with line items, partial payments, derived status",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.8.1",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
